---
import { BODA_INFO } from '../data/InfoBoda.js';
import { Image } from 'astro:assets';

// Asegúrate de que BODA_INFO.BotonSobre ahora apunte a tu archivo "deco-boton.jpg"
---

<div id="sobre-overlay" class="overlay">
  <!-- Fondo del sobre completo (sobre3.png) -->
  <Image src={BODA_INFO.sobre} alt="Sobre cerrado" class="sobre-bg" />

  <!-- Botón central redondo con el sello de cera (Capa superior) -->
  <button id="btn-abrir" class="boton-abrir">
    <Image src={BODA_INFO.BotonSobre} alt="Sello de cera" class="sello-imagen" />
  </button>
</div>

<style>
  /* --- CONTROL DEL SCROLL --- */
  :global(body) {
    overflow: hidden;
  }

  :global(body.scroll-permitido) {
    overflow: auto;
    overflow-x: hidden;
  }

  /* --- ESTILOS DEL SOBRE (PANTALLA COMPLETA) --- */
  .overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 99999;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: #f2f2f2; 
    transition: background-color 1.5s ease, visibility 1.5s ease;
  }

  /* Estilos para la imagen del sobre completo */
  /* Estilos para la imagen del sobre completo */
  .sobre-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    /* COVER: Mantiene las proporciones reales intactas. Las líneas no se deforman. */
    object-fit: cover; 
    /* CENTER: Clava el centro de la imagen justo detrás del sello sin importar la pantalla */
    object-position: center; 
    z-index: 1;
    transition: transform 1.5s cubic-bezier(0.25, 0.8, 0.25, 1);
    
    /* TRUCOS ANTI-DIENTES DE SIERRA */
    image-rendering: high-quality; 
    transform: translateZ(0); 
    will-change: transform; 
  }

  /* --- ESTILOS DEL BOTÓN REDONDO (EL SELLO) --- */
  .boton-abrir {
    position: relative;
    z-index: 100000;
    width: 150px; 
    height: 150px;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: transparent; 
    border: none;
    cursor: pointer;
    transition: transform 0.3s ease, opacity 0.5s ease;
    /* Quitamos el overflow:hidden, box-shadow y border-radius porque la imagen manda */
  }

  .boton-abrir:hover {
    transform: scale(1.05);
  }

  /* Estilos para que la imagen ocupe todo el botón */
  .sello-imagen {
    width: 100%;
    height: 100%;
    object-fit: cover;
    mix-blend-mode: multiply; /* ¡EL TRUCO MÁGICO QUE BORRA EL BLANCO! */
    filter: drop-shadow(0px 8px 12px rgba(0, 0, 0, 0.4)); /* Sombra realista 3D que sigue las curvas de la cera */
    transition: filter 0.3s ease;
  }

  .boton-abrir:hover .sello-imagen {
    filter: drop-shadow(0px 12px 18px rgba(0, 0, 0, 0.5)); /* La sombra crece al pasar el cursor */
  }

  /* -------------------------------------
     CLASES DE ANIMACIÓN (Cuando se abre)
     ------------------------------------- */
     
  .overlay.is-open {
    background-color: transparent; 
    pointer-events: none; 
  }

  /* El sobre se desliza hacia arriba */
  .overlay.is-open .sobre-bg {
    transform: translateY(-100%) translateZ(0);
  }

  /* El sello desaparece y se encoge */
  .overlay.is-open .boton-abrir {
    opacity: 0;
    transform: scale(0);
  }
</style>

<script>
  const btn = document.getElementById('btn-abrir');
  const overlay = document.getElementById('sobre-overlay');

  if (btn && overlay) {
    btn.addEventListener('click', () => {
      overlay.classList.add('is-open');
      document.body.classList.add('scroll-permitido');
      
      setTimeout(() => {
        overlay.style.display = 'none';
      }, 1500); 
    });
  }
</script>